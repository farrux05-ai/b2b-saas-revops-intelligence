# Database & dbt
duckdb
dbt-core
dbt-duckdb
dbt-postgres

# Utilities
python-dotenv
requests
scikit-learn
psycopg2-binary
dlt[motherduck]
elementary-data[duckdb]

# Orchestration
dagster
dagster-webserver
dagster-dbt

# Cloud Warehouse
motherduck

# Vector Engine (Step 3 — Unstructured data pipeline)
polars               # Fast columnar DataFrame: JSON loading + text cleaning
lancedb              # Local vector database (file-based, Arrow-native, DuckDB-compatible)
sentence-transformers # Local embedding model: BAAI/bge-small-en-v1.5
pyarrow              # Arrow schema support (required by LanceDB)
