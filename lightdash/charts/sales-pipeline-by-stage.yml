contentType: chart
name: Pipeline by Deal Stage (Weighted)
slug: sales-pipeline-by-stage
tableName: fct_pipeline
spaceSlug: sales
version: 1
metricQuery:
  exploreName: fct_pipeline
  dimensions:
    - fct_pipeline_deal_stage
  metrics:
    - fct_pipeline_weighted_pipeline_value
  filters: {}
  sorts: []
  tableCalculations: []
  limit: 500
chartConfig:
  type: cartesian
  config:
    eChartsConfig:
      series:
        - encode:
            xRef:
              field: fct_pipeline_deal_stage
            yRef:
              field: fct_pipeline_weighted_pipeline_value
          type: bar
    layout:
      xField: fct_pipeline_deal_stage
      yField:
        - fct_pipeline_weighted_pipeline_value
