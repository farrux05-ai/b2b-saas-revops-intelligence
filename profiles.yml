# ============================================================
# dbt Profile — RevOps Intelligence Engine
#
# TARGETS:
#   local     → DuckDB file (fast local dev, testing, CI)
#   snowflake → Snowflake (production enterprise warehouse)
#   ci        → DuckDB in-memory (GitHub Actions)
#
# USAGE:
#   dbt run                          # uses 'local' (default)
#   dbt run --target snowflake       # Snowflake (production)
#   dbt run --target ci              # CI in-memory
# ============================================================

revops_intelligence_engine:
  target: local
  outputs:

    # ── LOCAL DEV / TESTING — DuckDB file ────────────────────
    local:
      type: duckdb
      path: '/home/farrux/data_projects/b2b-saas-revops/duckdb/revops_intelligence.duckdb'
      threads: 4

    # ── PRODUCTION — Snowflake ───────────────────────────────
    # Set credentials in .env or Dagster Cloud Env Vars:
    #   SNOWFLAKE_ACCOUNT, SNOWFLAKE_USER, SNOWFLAKE_PASSWORD
    #   SNOWFLAKE_ROLE, SNOWFLAKE_WAREHOUSE, SNOWFLAKE_DATABASE
    snowflake:
      type: snowflake
      account: "{{ env_var('SNOWFLAKE_ACCOUNT') }}"
      user: "{{ env_var('SNOWFLAKE_USER') }}"
      password: "{{ env_var('SNOWFLAKE_PASSWORD') }}"
      role: "{{ env_var('SNOWFLAKE_ROLE', 'TRANSFORMER') }}"
      warehouse: "{{ env_var('SNOWFLAKE_WAREHOUSE', 'COMPUTE_WH') }}"
      database: "{{ env_var('SNOWFLAKE_DATABASE', 'REVOPS_INTELLIGENCE') }}"
      schema: MARTS
      threads: 8
      client_session_keep_alive: false
      query_tag: "dbt_revops_intelligence"

    # ── CI — In-memory DuckDB ────────────────────────────────
    ci:
      type: duckdb
      path: ":memory:"
      threads: 4

# ── Elementary observability — mirrors main profile targets ──
elementary:
  target: local
  outputs:
    local:
      type: duckdb
      path: '/home/farrux/data_projects/b2b-saas-revops/duckdb/revops_intelligence.duckdb'
      schema: main_elementary
      threads: 4
    snowflake:
      type: snowflake
      account: "{{ env_var('SNOWFLAKE_ACCOUNT') }}"
      user: "{{ env_var('SNOWFLAKE_USER') }}"
      password: "{{ env_var('SNOWFLAKE_PASSWORD') }}"
      role: "{{ env_var('SNOWFLAKE_ROLE', 'TRANSFORMER') }}"
      warehouse: "{{ env_var('SNOWFLAKE_WAREHOUSE', 'COMPUTE_WH') }}"
      database: "{{ env_var('SNOWFLAKE_DATABASE', 'REVOPS_INTELLIGENCE') }}"
      schema: ELEMENTARY
      threads: 4
      query_tag: "elementary_revops"
    ci:
      type: duckdb
      path: ":memory:"
      threads: 4
